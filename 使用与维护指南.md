# 100cuci.ad 网站 — 使用与维护指南

> 整理日期：2026年7月  
> 项目：100CUCI Landing Page  
> 线上域名：https://100cuci.ad  
> GitHub：https://github.com/hyperosa001/100cuci-site  
> Register 链接：https://amanahmy.com/RFSEO0001  

---

## 一、项目在哪里

**本地文件夹（源代码）：**
```
C:\Users\user-notebook\Desktop\ai 克隆网站工具\ai-website-cloner-template-master
```

**三层保存：**

| 层级 | 作用 |
|------|------|
| 本地文件夹 | 日常改代码 |
| GitHub (100cuci-site) | 代码备份 + 触发自动部署 |
| Vercel | 线上网站 100cuci.ad |

建议：整个项目文件夹可复制到 U 盘 / 网盘 / OneDrive 做额外备份。

---

## 二、以后要改什么，改哪个文件

| 想改的内容 | 怎么改 |
|-----------|----------|
| **栏目文章** | WordPress 发文 → `npm run build` / Vercel Redeploy（见 `docs/GO-LIVE.md`） |
| **Login / Register** | WP「100CUCI Site Links」或 `content/site-links.json` |
| **Yoast SEO 标题描述** | WP 文章里填 Yoast → 重新 build |
| **底部 SEO / FAQ** | `/admin` 或 `content/homepage-seo.json` |
| **样式 / Banner** | `src/app/landing.css` / `LandingPage.tsx` |
| **环境变量** | Vercel：`WP_REST_URL`、`NEXT_PUBLIC_SITE_URL` |

---

## 三、标准更新流程（改完 → 上线）

### 1. 本地预览（推荐先做）

打开 PowerShell：

```powershell
cd "C:\Users\user-notebook\Desktop\ai 克隆网站工具\ai-website-cloner-template-master"
$env:Path = "C:\Program Files\nodejs;" + $env:Path
npm run dev
```

浏览器打开：**http://localhost:3000**  
手机预览：F12 → 手机图标，或同一 WiFi 下访问终端显示的 Network 地址。

### 2. 推送到 GitHub（自动部署到 100cuci.ad）

```powershell
cd "C:\Users\user-notebook\Desktop\ai 克隆网站工具\ai-website-cloner-template-master"

git add .

git commit -m "这里写你改了什么，例如：Update register link"

git push
```

如果提示找不到 `git`，用完整路径：

```powershell
& "C:\Program Files\Git\bin\git.exe" add .
& "C:\Program Files\Git\bin\git.exe" commit -m "Update something"
& "C:\Program Files\Git\bin\git.exe" push
```

### 3. 等 Vercel 部署

1. 打开 https://vercel.com → 项目 **100cuci-site**
2. **Deployments** → 等最新一条变绿色 **Ready**（约 2–3 分钟）
3. 打开 https://100cuci.ad ，**Ctrl+F5** 强制刷新

---

## 四、常见修改示例

### 换 Register / Login 链接

编辑 `src/config/site-links.ts`：

```ts
export const SITE_LINKS = {
  login: "https://你的链接",
  register: "https://你的链接",
} as const;
```

保存 → `git add .` → `git commit -m "Update links"` → `git push`

### 更新 SEO 文章（建议每月或每季度）

编辑 `src/content/seo-content.ts`：

- 改 `SEO_LAST_UPDATED` 的日期
- 改 `SEO_SECTIONS` 和 `SEO_FAQ` 里的文字

保存 → push → Vercel 自动部署

### 换 Banner 或游戏图片

1. 新图片放进 `public/media/`
2. 在 `LandingPage.tsx` 里改对应的 `/media/xxx.png` 路径
3. push

---

## 五、不要提交到 GitHub 的文件

已在 `.gitignore` 里，**不会**被 push：

- `.env.local`（本地环境变量）
- `node_modules/`

**Vercel 环境变量**（在 Vercel 网站设置，不在代码里）：

| 变量名 | 当前值 |
|--------|--------|
| `NEXT_PUBLIC_SITE_URL` | `https://100cuci.ad` |
| `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` | Google 验证码 |
| `WORDPRESS_URL` | WordPress 根地址，如 `https://cms.100cuci.ad`（栏目文章用） |

---

## 六、换电脑时怎么做

1. 安装 Git：https://git-scm.com  
2. 安装 Node.js：https://nodejs.org  
3. 克隆项目：

```powershell
git clone https://github.com/hyperosa001/100cuci-site.git
cd 100cuci-site
npm install
```

4. 新建 `.env.local`：

```
NEXT_PUBLIC_SITE_URL=https://100cuci.ad
```

5. `npm run dev` 本地预览 → 改代码 → `git push`

---

## 七、Google SEO 相关

| 项目 | 地址 / 说明 |
|------|-------------|
| Google Search Console | https://search.google.com/search-console |
| 已验证域名 | https://100cuci.ad |
| Sitemap | https://100cuci.ad/sitemap.xml |
| robots.txt | https://100cuci.ad/robots.txt |

**SEO 建议：**
- 底部文章保持 800 字以上
- 定期更新 `seo-content.ts`（改日期 + 内容）
- 收录通常需要 1–7 天，新站可能更长

---

## 八、域名与 DNS（Namecheap）

- 域名：**100cuci.ad**
- DNS 已指向 Vercel
- 如需改 DNS：Namecheap → Domain List → 100cuci.ad → Manage

---

## 九、网站架构说明

当前线上是 **Landing Page**（官方风格单页）：

- 用户浏览 100cuci.ad → 看到推广页
- 点击 Register / Login → 跳转到 `site-links.ts` 里的链接
- 注册后才进入实际游戏平台

旧版完整克隆站代码仍在 `src/clone/` 文件夹，如需恢复可改 `src/app/page.tsx` 改回 `CloneSite` 组件（一般不需要）。

---

## 十、遇到问题

| 问题 | 处理 |
|------|------|
| `git` 找不到 | 重装 Git 或重启 PowerShell，用 `C:\Program Files\Git\bin\git.exe` |
| localhost 打不开 | 确认 `npm run dev` 在运行，用 http://localhost:3000 |
| 线上还是旧版 | Vercel Deployments 看是否 Ready，Ctrl+F5 刷新 |
| 改链接没生效 | 确认改了 `site-links.ts` 并已 push |

---

## 十一、快速命令备忘

```powershell
# 进入项目
cd "C:\Users\user-notebook\Desktop\ai 克隆网站工具\ai-website-cloner-template-master"

# 本地预览
npm run dev

# 上线
git add .
git commit -m "说明改动"
git push
```

---

*此文档由 Cursor 对话整理，供日后维护 100cuci.ad 参考。*
